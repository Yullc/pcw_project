<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.repository.ChatRepository">

    <insert id="insertMessage" parameterType="org.example.vo.ChatMessage">
        INSERT INTO teamChatMessage (teamId, memberId, message,nickName,sendTime)
        VALUES ( #{teamId}, #{memberId}, #{message}, #{nickName},NOW())
    </insert>



    <insert id="save" parameterType="org.example.vo.ChatMessage">
        INSERT INTO teamChatMessage (teamId, memberId, message, sendTime)
        VALUES (#{teamId}, #{memberId}, #{message}, NOW())
    </insert>


</mapper>

