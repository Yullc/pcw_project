<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.repository.ReactionPointRepository">

    <select id="getReactionCount" resultType="int">
        SELECT COUNT(*)
        FROM reactionPoint
        WHERE memberId = #{fromMemberId}
          AND toMemberId = #{toMemberId}
    </select>

    <insert id="insertReactionPoint">
        INSERT INTO reactionPoint
        SET regDate = NOW(),
            updateDate = NOW(),
            memberId = #{fromMemberId},
            toMemberId = #{toMemberId},
            point = #{point}
    </insert>

    <delete id="deleteReactionPoint">
        DELETE FROM reactionPoint
        WHERE memberId = #{fromMemberId}
          AND toMemberId = #{toMemberId}
    </delete>

</mapper>
